{
  "name": "amico-fakestore",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT db.json"
  }
}
